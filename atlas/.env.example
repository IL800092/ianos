# Optional — enables the ATLAS chat panel.
# The key stays server-side: the Express proxy calls the Anthropic API,
# the browser never sees it.
ANTHROPIC_API_KEY=

# Ports (defaults shown)
# ATLAS_API_PORT=5178
